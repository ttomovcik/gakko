/**
 * Load website into iframe container with CORS bypass
 * ? Get array from settings
 * ! FUCKING CORS BYPASS
 * 
 * TODO: Add class with constructor (iframe, Array[])
 * TODO: Idk fam, just make it work
 */