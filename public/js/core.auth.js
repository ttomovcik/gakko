function login(m, p) {
    let mail = m;
    let password = p;
}

function logout(guid) {
    let userID = guid;
}

function register(m, p) {
    let mail = m;
    let password = p;
}

function resetPassword(m) {
    let mail = m;
}